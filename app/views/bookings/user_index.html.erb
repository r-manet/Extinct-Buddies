<h1>Your bookings!</h1>
<ol>
  <div>
    <% @bookings.each do |item| %>
      <li>Your booking for <%= item.animal.name%> is from the <%= item.start_date %> to the <%=item.end_time  %></li>
    <% end %>
  </div>
</ol>
